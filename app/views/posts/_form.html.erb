	<%= simple_form_for @post do |f| %>
		<%= f.input :title %>
		<%= f.input :link %>
		<span><%= f.input :image %></span>
		<br>
		<%= f.input :description, label: false, placeholder: "Tell us about the link!" %>
		<br>
		<div class="form-button">
			<%= f.button :submit %>
		</div>
	<% end %>