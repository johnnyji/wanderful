$(document).ready(function() {
	<% if @post.errors.any? %>
 		window.location.replace("<% new_post_path %>");
	<% else %>
		$(".post-container").prepend("<%= j (render @post) %>");
		$(".index-form").slideUp(400);
	<% end %>
});