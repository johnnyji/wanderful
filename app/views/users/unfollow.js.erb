$(document).ready(function() {
	var $buttonDiv = $(".follow-button");
	var $counterFollowers = $(".follower-count");
	var $counterFollowing = $(".following-count");

	$buttonDiv.html("<%= j (render partial: 'follow')%>");

	$counterFollowers.html("<%= j (render partial: 'followers') %>");
	$counterFollowing.html("<%= j (render partial: 'following') %>");
});